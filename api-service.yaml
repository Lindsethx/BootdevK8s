apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  type: ClusterIP
  selector:
    app: synergychat-api
  ports:
    - protocol: TCP
      port: 80              #port that service will listen on
      targetPort: 8080 #port that pods are listening on
