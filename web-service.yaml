apiVersion: v1
kind: Service
metadata:
  name: web-service
spec:
  selector:
    app: synergychat-web
  ports:
    - protocol: TCP
      port: 80              #port that service will listen on
      targetPort: 8080 #port that pods are listening on
